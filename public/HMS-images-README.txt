Our Work page uses these images. Add them to the public folder (this directory):

  - HMS-mobile.jpeg   (mobile screenshot)
  - HMS-pc.png        (desktop/PC screenshot)

Use these exact names (with hyphen, no space) so the desktop and mobile images load.
